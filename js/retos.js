
/*function suma(y,z){
    var x;
    x = y+z;
    return x;
  }
  console.log (suma(245,923));

// otra forma

  function suma(){
    let x;
    x = 1+1;
    console.log(x);
  }
  
  suma()

  // otra
  function suma (y,z){
    var x;
    x = y+z;
    console.log(x);
  }
  suma(1,2);
  suma(0,0);
  suma(245,923);
 

 function suma(y, z){
  return y+z;
}
console.log (suma(245,923));

  function saluda (nombre){
    return  `"hola ${nombre}"` 
  }

  console.log (saluda("Jefry Vasquez")); 

  function longitud (txt){
    return txt.length;
  }

  console.log (longitud ("hola"));*/





  function isAlive(playerName, points) {
    return (playerName === "ikk" && points > 30) || (playerName === "gut" && points > 10)
  }

  console.log(isAlive ("gutp", 50));